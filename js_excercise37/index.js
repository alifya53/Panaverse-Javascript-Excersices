    function make_shirt(size, message) {
        if (size === "large" || size == "medium") {
            console.log(`the size of the shirt is ${size} and message say ${message}, BraveGuy`)
        } else {
            console.log(`shirts of size ${size} are you Cool`)
        }
    }
    make_shirt('small',"Hello Handsome");

