let add=5+3;   // addition operator used and storing the result  in variable add
let sub=8-0;   // subraction operator used and storing the result  in variable sub
let mul=1*8;   // multiplication operator used and storing the result  in variable mul
let div=8/1;   // division operator used and storing the result in variable div

// printing the result of addand so on
console.log(add);  
console.log(sub);
console.log(mul);
console.log(div);